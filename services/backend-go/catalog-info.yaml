apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backend-go
  description: Go backend service (monorepo subcomponent)
  annotations:
    pantalasa.org/domain: engineering.platform.core
  tags:
    - go
    - service
spec:
  type: service
  lifecycle: production
  owner: group:default/team-backend
  # TEMP (ENG-1654 / lunar-lib#297): exercises the backstage collector's
  # referential-integrity lookups against the in-cluster Backstage.
  # `core-platform` is a real Domain in the seed catalog (expect exists: true);
  # `typo-platform-eng1654` deliberately is not (expect exists: false).
  # REVERT once the test is recorded.
  domain: core-platform
  system: typo-platform-eng1654
